# victorgilalejandria.github.io

Personal site / resume on GitHub Pages (Jekyll).

## Local preview (optional)
If you have Ruby + Bundler:

```bash
bundle exec jekyll serve
```

Deployed via GitHub Pages.
